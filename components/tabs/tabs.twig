{#
/**
 * @file
 * Tabs.
 */
#}
{% include '@ecl-twig/tabs' with {
  items: items,
  more_label: more_label|default('More'|t),
  previous_label: previous_label,
  next_label: next_label,
  extra_attributes: extra_attributes,
  extra_classes: extra_classes
} only %}
