name: ci
on:
  pull_request:
    types: [opened, synchronize]
  push:
    branches:
      - 6.x

jobs:
  automated-tests:
    uses: openeuropa/github-ci/.github/workflows/drupal-ci.yml@EWPP-6781
    with:
      run_phpunit_batches: true
      run_behat: true
