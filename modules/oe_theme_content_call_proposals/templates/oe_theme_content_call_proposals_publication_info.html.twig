{#
/**
 * @file
 * Theme for the publication details extra field on Call for proposals content type.
 *
 * @see \Drupal\oe_theme_content_call_proposals\Plugin\ExtraField\Display\CallForProposalsPublicationInfoExtraField::viewElements()
 */
#}
{# Such formatting is required to support comma between date and link in translations. #}
{{ date }}{% if link is not empty %}{% trans ' in' with {'context': 'Call for proposals'} %}
  {% include '@ecl-twig/link' with {
    link: {
      type: 'standalone',
      label: link.title,
      path: link.url,
      icon_position: 'after'
    },
    icon: get_link_icon(link.url)
  } %}
{% endif %}
