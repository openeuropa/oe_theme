{#
/**
 * @file
 * Theme override for the timeline field template.
 */
#}
{{ pattern('timeline', {
  'limit': limit,
  'limit_to': items | length - 1,
  'button_label': show_more,
  'items': items,
}) }}
