{#
/**
 * @file
 * Theme override for the timeline field template.
 */
#}
<div class="ecl-editor ecl-u-mv-m">
  {{ pattern('timeline', {
    'limit': limit,
    'button_label': show_more,
    'items': items,
  }) }}
</div>
