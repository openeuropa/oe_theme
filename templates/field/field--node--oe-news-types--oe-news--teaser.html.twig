{#
/**
 * @file
 * Theme override for news type field template on teaser view mode.
 *
 * @see ./core/themes/stable/templates/field/field.html.twig
 */
#}
{% for item in items %}
  {{- item.content -}}{{ not loop.last ? ', ' }}
{% endfor %}
