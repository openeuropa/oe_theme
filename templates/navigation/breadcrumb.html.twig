{#
/**
 * @file
 * Theme override for a breadcrumb trail.
 *
 * Available variables:
 * - breadcrumb: Breadcrumb trail items.
 */
#}
{% if breadcrumb %}
  {% include '@ecl-twig/twig-component-breadcrumb-' ~ ecl_branding ~ '/breadcrumb-' ~ ecl_branding ~ '.html.twig' %}
{% endif %}
