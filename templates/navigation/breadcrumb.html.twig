{#
/**
 * @file
 * Theme override for a breadcrumb trail.
 *
 * Available variables:
 * - breadcrumb: Breadcrumb trail items.
 */
#}
{% if breadcrumb %}
  {% include '@ec-europa/ecl-breadcrumb' with {
    'segments': segments,
  } %}
{% endif %}
