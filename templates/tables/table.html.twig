{#
/**
 * @file
 * Table themed using the ECL table component.
 */
#}
{% include '@ecl-twig/twig-component-table/table.html.twig' with {
  'zebra': zebra|default(false),
  'headers': headers,
  'rows': rows,
  'extra_classes': extra_classes,
  'extra_attributes': extra_attributes,
} only %}
