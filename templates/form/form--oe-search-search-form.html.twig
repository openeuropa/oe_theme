{#
/**
 * @file
 * Theme override for a 'form' element.
 *
 * @see ./core/themes/stable/templates/form/form.html.twig
 * @see @ecl/search-form.twig
 *
 * In order to avoid usage of not secure form template in ecl we have to partically copy code from @ecl/search-form.twig
 */
#}

<form {{ attributes.addClass(['ecl-search-form', 'ecl-search-form--']) }}>
  <label class="ecl-search-form__textfield-wrapper">
    <span class="ecl-u-sr-only">{{ "Search this website" }}</span>
    {% include '@ecl/form-text-input' with {
      'id': 'search',
      'type': 'search',
      'extra_classes': 'ecl-search-form__textfield'
    }|merge(input) only %}
  </label>
  {% include '@ecl/button' with {
    'label': label|default('Search'|t),
    'modifier': 'form',
    'extra_classes': 'ecl-search-form__button'
  } %}
  {{ element.form_build_id }}
  {{ element.form_token }}
  {{ element.form_id }}
</form>
