{#
/**
 * @file
 * Theme override for an 'input' #type form element.
 *
 * @see ./core/themes/stable/templates/form/input.html.twig
 */
#}
{%
  set classes = [
    'ecl-text-input',
    attributes.hasClass('error') ? 'ecl-text-input--has-error',
]
%}
<input{{ attributes.addClass(classes) }} />{{ children }}
