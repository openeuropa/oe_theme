{#
/**
 * @file
 * Theme override for an 'input' #type form element.
 *
 * Available variables:
 * - attributes: A list of HTML attributes for the input element.
 * - children: Optional additional rendered elements.
 * - element: Array that represents the element shown in the form.
 * - input_array: Array that contains all attributes of the element shown in the form.
 * - ecl_icon_path: Path to the ECL icon file.
 *
 * @see ./core/themes/stable/templates/form/input.html.twig
 */
#}

{% include '@ecl-twig/form-group' with {
  input: {
    input_type: 'search',
    icon_path: ecl_icon_path,
    id: input_array.id,
    name: input_array.name,
    invalid: input_array.invalid,
    disabled: input_array.disabled,
    required: element['#required'],
    extra_classes: input_array.class|safe_join(' '),
    extra_attributes: input_array.extra_attributes,
  },
  invalid: input_array.invalid,
  disabled: input_array.disabled,
  extra_classes: input_array.class|safe_join(' '),
  extra_attributes: input_array.extra_attributes,
  icon_path: ecl_icon_path,
} %}{{ children }}
