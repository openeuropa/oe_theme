{#
/**
 * @file
 * Theme override for an 'input' #type form element.
 *
 * Available variables:
 * - attributes: A list of HTML attributes for the input element.
 * - children: Optional additional rendered elements.
 * - disabled: Optional (boolean): is disabled (optional, default: false).
 * - invalid: Optional (boolean): has error (optional, default: false).
 *
 * @see ./core/themes/stable/templates/form/input.html.twig
 */
#}
{% include '@ecl-twig/form-group' with {
  input: {
    input_type: 'checkbox',
    id: attributes.id,
    name: attributes.name,
    items: [
      {
        value: attributes.value,
        checked: attributes.checked is defined,
        id: attributes.id,
        name: attributes.name,
        label: title,
        icon_path: ecl_icon_path,
        disabled: attributes.disabled is defined,
      }
    ]
  },
  invalid: invalid,
  disabled: attributes.disabled is defined,
  extra_attributes: input_attributes|default([]),
  required: attributes.required is defined,
  label_aria_required: attributes.required is defined,
  icon_path: ecl_icon_path,
} %}{{ children }}
