{#
/**
 * @file
 * Teaser template implementation for "Consultation" content type.
 */
#}
{{ dump(content.group_details) }}
{{ pattern('list_item', {
  'variant': 'default',
  'url': url,
  'title': label,
  'meta': [
    content.extra_field_oe_consultation_label_status|field_value,
  ],
  'additional_information': content.group_details
}) }}
