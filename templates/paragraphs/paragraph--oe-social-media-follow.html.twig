{#
/**
 * @file
 * Theme override to display the 'social media follow' type paragraph.
 *
 * @see ./modules/contrib/paragraphs/templates/paragraph.html.twig
 */
#}
{% if variant.value == 'horizontal' %}
  {{ pattern('social_media_links_horizontal', {
    'title': label,
    'links': links,
  }) }}
{% else %}
  {{ pattern('social_media_links_vertical', {
    'title': label,
    'links': links,
  }) }}
{% endif %}

