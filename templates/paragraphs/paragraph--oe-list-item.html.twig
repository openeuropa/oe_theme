{#
/**
 * @file
 * Theme override to display the 'list item' type paragraph.
 *
 * @see ./modules/contrib/paragraphs/templates/paragraph.html.twig
 */
#}
{% if variant == 'highlight' %}
  {# Use featured_item pattern as the highlight variant has been deprecated in 2.9.0 #}
  {{ pattern('featured_item', {
    'variant': 'default',
    'image': image,
    'title': content.field_oe_title,
    'link': { 'href': url },
  }) }}
{% else %}
  {{ pattern('list_item', {
    'variant': variant,
    'url': url,
    'title': content.field_oe_title,
    'detail': content.field_oe_text_long,
    'meta': meta,
    'date': date,
    'image': image,
    'secondary_image': image
  }) }}
{% endif %}
