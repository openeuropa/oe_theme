{#
/**
 * @file
 * Default implementation for the highlight list item component.
 */
#}
{% include '@ecl/card' with {
  card : {
    title: {
      label: title,
      path: url,
    },
    image: image.src
  }
} only %}
