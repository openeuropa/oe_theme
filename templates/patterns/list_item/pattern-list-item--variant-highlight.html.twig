{#
/**
 * @file
 * Default implementation for the highlight list item component.
 */
#}
{% include '@ecl-twig/card' with {
  card: {
    title: {
      label: title,
      path: url,
      type: "standalone"
    },
    image: image,
    description: detail,
  }
} only %}
