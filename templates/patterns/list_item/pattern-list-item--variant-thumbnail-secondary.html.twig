{#
/**
 * @file
 * Default theme implementation for a ECL thumbnail list item component.
 */
#}
{% include '@oe_theme/patterns/list_item/pattern-list-item.html.twig' with {
  title: title,
  url: url,
  detail: detail|smart_trim(length),
  image: image,
  image_position: 'after',
  meta: meta,
  field_list: field_list
} only %}
