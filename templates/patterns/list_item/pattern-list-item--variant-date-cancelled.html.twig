{#
/**
 * @file
 * Cancelled event list item component.
 */
#}
{% include '@oe_theme/patterns/list_item/pattern-list-item--variant-date.html.twig' with {
  title: title,
  url: url,
  date: date,
  detail: detail,
  length: length,
  meta: meta,
  field_list: field_list,
  variant: 'cancelled'
} only %}
