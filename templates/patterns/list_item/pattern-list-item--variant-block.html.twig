{#
/**
 * @file
 * Default implementation for the block list item component.
 */
#}
{% include '@ecl-twig/twig-component-card/card.html.twig' with {
  card : {
    title: {
      label: title,
      path: url,
      type: "standalone"
    },
    description: detail|smart_trim(length)
  }
} only %}
