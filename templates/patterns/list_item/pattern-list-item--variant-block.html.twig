{#
/**
 * @file
 * Default implementation for the block list item component.
 */
#}
{% include '@ecl-twig/card' with {
  card : {
    title: {
      label: title,
      path: url,
      type: "standalone"
    },
    description: smart_trim(detail, length)
  }
} only %}
