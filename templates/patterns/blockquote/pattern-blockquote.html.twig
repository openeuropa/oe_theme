{#
/**
 * @file
 * Link block.
 */
#}
{% if image.src is not empty %}
  {% set _picture = {
    img: {
      'src': image.src,
      'alt': image.alt
    }
  } %}
{% endif %}
{% include '@ecl-twig/blockquote' with {
  'author': author,
  'citation': body,
  'picture': _picture|default({}),
} %}
