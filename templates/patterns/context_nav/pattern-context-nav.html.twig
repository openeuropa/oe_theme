{#
/**
 * @file
 * Default implementation for the contextual navigation component.
 */
#}
{% include '@ecl/ec-component-context-nav' with {
  label: label,
  items: items,
  limit: limit,
  more_label: more_label|default('More'|t)
} only %}
