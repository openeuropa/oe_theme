{#
/**
 * @file
 * Field.
 */
#}

{% set _items = [] %}
{% set _items = _items|merge([{
  term: label,
  definition: body
}]) %}

{% include '@ecl-twig/twig-component-description-list/description-list.html.twig' with {
  'variant': variant,
  'items': _items,
} %}

