{#
/**
 * @file
 * Field.
 */
#}

{% set _items = [] %}
{% set _items = _items|merge([{
  term: label,
  definition: body
}]) %}

{% include '@ecl/description-list' with {
  'variant': variant,
  'items': _items,
} %}

