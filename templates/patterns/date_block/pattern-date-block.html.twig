{#
/**
 * @file
 * Default implementation for the date block component.
 */
#}
{% include '@ecl/ec-component-date-block' with {
  variant: variant|replace({'_':'-'}),
  week_day: date.week_day,
  day: date.day,
  month: date.month_name,
  year: date.year
} only %}
