{#
/**
 * @file
 * Default implementation for the hero banner component.
 */
#}
{% include '@ecl/hero-banner' with {
  type: banner_type|e,
  image: image.src,
  title: title,
  link: {'link' : {'path' : url, 'label' : label}},
  description: description,
  centered: centered
} only %}
