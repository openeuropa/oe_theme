{#
/**
 * @file
 * Banner hero.
 */
#}

{% set _link = {'link' : {'path' : url, 'label' : label}} %}

{% include '@ecl-twig/hero-banner' with {
  'type': banner_type|e,
  'image': image.src,
  'title': title,
  'link': _link,
  'description': description,
  'centered': centered,
} %}
