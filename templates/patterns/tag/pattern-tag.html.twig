{#
/**
 * @file
 * Tag.
 */
#}
{% if variant == 'default' %}
  {% include '@ecl-twig/twig-component-tag/tag.html.twig' with {
    'tag': {
      'type': tag_type,
      'path': url,
      'label': label,
    }
  } %}
{% endif %}
